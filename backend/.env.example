# Copy this file to .env and fill in values. The app runs fully with all
# values left blank: LLM calls silently degrade to deterministic templates.

GEMINI_API_KEY=
GROQ_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
GROQ_MODEL=llama-3.1-8b-instant
PREFERRED_LLM_PROVIDER=gemini
